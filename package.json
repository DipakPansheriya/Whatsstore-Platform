{
  "name": "whatsstore-platform",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install --include=dev",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm run start"
  }
}
